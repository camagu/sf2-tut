{% macro group(alerts, type) %}
    {% import _self as self %}

    {% for alert in alerts %}
        {{ self.single(alert, type) }}
    {% endfor %}
{% endmacro %}

{% macro single(alert, type) %}
    <div class="alert alert-{{type|default('success')}}">{{ alert }}</div>
{% endmacro %}
