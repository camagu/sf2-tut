{% import "SanchoAppBundle:Macros:alerts.html.twig" as alerts %}

{% set flashBag = app.session.flashBag %}
{{ alerts.group(flashBag.get('success'), 'success') }}
{{ alerts.group(flashBag.get('error'), 'error') }}
