{% extends 'SanchoAppBundle::layout.html.twig' %}
{% form_theme form 'SanchoAppBundle:Form:form.html.twig' %}

{% block title %}{{ full_title('Sign up') }}{% endblock %}

{% block body %}
    <h1>Sign up</h1>
    <div class="row">
        <div class="span6 offset3">
            {{ form(form, { attr: { novalidate: "novalidate" } }) }}
        </div>
    </div>
{% endblock %}
